hikari:
  config:
    connection-timeout: 60000
    connectionTestQuery: SELECT 1
    dataSource:
      password: root
      url: jdbc:mysql://localhost:3306/db_quizard?createDatabaseIfNotExist=true&serverTimezone=UTC
      user: root
    dataSourceClassName: com.mysql.cj.jdbc.MysqlDataSource
    idleTimeout: 30000
    maximumPoolSize: 20
    minimumIdle: 5
    poolName: quizard_con_pool
    transactionIsolation: TRANSACTION_REPEATABLE_READ
#RabbitMQ Properties
rabbitmq:
  host:
  port:
  username:
  password:
#Email Settings Properties
quizard:
  email:
    sender:
      user:
      password:
      debug:
mail:
  transport:
    protocol:
  smtp:
    auth:
    starttls:
      enable:
  debug: